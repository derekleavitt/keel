---
id: L-001
class: contradiction
enforced_by: test
enforcement_ref: packages/db/src/schema/schema.test.ts
observed: 2026-08-27
---
# schema.ts header comment contradicted .claude/rules/database.md

Six agents read a file whose own header said the opposite of the rule governing it.
Two nearly defined a table in a feature package, which hard-fails Turbo with a workspace
cycle.

Prose in two places cannot stay consistent. A test asserting the schema module exports
exactly the expected table set makes the wrong placement fail immediately, whatever any
comment claims.
