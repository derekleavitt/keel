---
id: P4-05
status: predicted
territory: orchestration
phase: 4
depends_on: [P4-01, P1-03]
verify: ["pnpm verify"]
---
# graph_disjoint for safe fan-out

Compute whether proposed tasks' blast radii overlap, before spawning.

Regression fixture: run 1's three tasks MUST be reported as overlapping. It is the known
answer — 8 shared files, 2 of 3 merges conflicted.

## Acceptance

- [ ] Reports run-1's three tasks as overlapping, naming schema.ts and the contracts barrel
- [ ] Reports genuinely independent tasks as disjoint
- [ ] Exposed over MCP
- [ ] Recommends a serialization order when overlap is found

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
