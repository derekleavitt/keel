---
id: P2-04
status: predicted
territory: docs
phase: 2
depends_on: [P2-01]
verify: ["pnpm verify"]
---
# Generated documentation

Derived tier: route map, per-package export surface, DB schema, env inventory,
dependency graph. Regenerated, never hand-edited.

## Acceptance

- [ ] `pnpm docs generate` writes docs/generated/
- [ ] Output is deterministic — running twice produces no diff
- [ ] docs/generated/ carries a header saying it is generated
- [ ] Gate fails if generated docs are stale relative to source

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
