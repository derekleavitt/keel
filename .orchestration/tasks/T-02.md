---
id: T-02
status: done
kind: feature
territory: testbed
depends_on: [T-01]
verify: ["pnpm verify"]
---
# Lists: create, rename, delete

The first real feature package. Everything else hangs off a list.

## Acceptance

- [x] `testbed/lists` package following the `examples/notes` pattern exactly
- [x] Table in `packages/db/src/schema/list.ts`, two lines added to `schema/index.ts`
- [x] Query helpers take branded `UserId` first, handle last with a `db()` default
- [x] PGlite tests with two users, cross-user isolation on every operation
- [x] Routes under `/lists` that a person can actually use

## Watch for

First test of whether `examples/notes` is a sufficient reference. If the agent still
invents its own query seam, the example failed and needs work — that is a Keel task.

This section is a hypothesis, not a specification. If the friction lands somewhere else,
that is the more interesting result — record it.
