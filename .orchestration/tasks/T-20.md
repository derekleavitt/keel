---
id: T-20
status: done
kind: feature
capability: realtime
territory: testbed
depends_on: [T-11]
verify: ["pnpm verify"]
---
# Real-time list updates

Two tabs disagreeing is a bug users report.

Proves the **realtime** row in `docs/platform-readiness.md`. A capability is not done until
the testbed genuinely uses it, a worked example exists for it, the gate covers it, and the
vendor behind it could be swapped without touching feature code.

## Acceptance

- [x] A change in one session appears in another without a refresh
- [x] Subscriptions are authorization-checked, not just filtered client-side
- [x] Reconnects recover missed state
- [x] Degrades to polling rather than breaking

## Watch for

First stateful connection. Expect the server-action model to have nothing to say
about it.

A hypothesis, not a specification. If the friction lands elsewhere, that is the more
interesting result — record it.

## Outcome

The server-action model had nothing to say about it for a specific reason: it authorizes
once per request, and a stream is a decision that has to keep being made. See
`.orchestration/journal/2026-08-28-T-20.md`, [[L-042]] and [[L-043]].
