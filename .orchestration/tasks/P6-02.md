---
id: P6-02
status: predicted
territory: loop
phase: 6
depends_on: [P2-01, P5-01]
verify: ["pnpm verify"]
---
# Requirement traceability and termination

req: nodes linked to implementing code. This is the loop's stopping rule — done is
every requirement having satisfied coverage, not an agent feeling finished.

## Acceptance

- [ ] req: nodes resolve and link to symbols and tests
- [ ] "which requirements have no implementation" answerable as a query
- [ ] Termination condition computable and used by the loop
- [ ] Regression: run against docs/prd.md, correctly reports the unbuilt agenda view

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
