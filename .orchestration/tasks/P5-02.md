---
id: P5-02
status: predicted
territory: initiation
phase: 5
depends_on: [P5-01]
verify: ["pnpm verify"]
---
# Domain model and schema proposal

Entities to Drizzle tables, following the spread-assembly convention.

## Acceptance

- [ ] Proposes tables, columns, relations from an approved domain model
- [ ] Uses the per-feature spread pattern
- [ ] Regression: given docs/prd.md, proposes list/todo/tag/todo_tag with correct cascades
- [ ] Output is reviewed before anything is written

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
