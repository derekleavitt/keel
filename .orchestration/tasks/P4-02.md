---
id: P4-02
status: predicted
territory: orchestration
phase: 4
depends_on: [P4-01]
verify: ["pnpm verify"]
---
# PreToolUse territory guard

Block writes outside the acting agent's territory. Exit 2 with a message naming the
owning territory and how to hand off.

## Acceptance

- [ ] Blocks an out-of-territory write
- [ ] Allows additive edits to shared-service packages
- [ ] Error text tells the agent how to file a handoff task
- [ ] Does not fire when no territory is active (solo work stays unimpeded)

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
