---
id: P1-03
status: open
territory: graph
phase: 1
depends_on: [P1-02]
verify: ["pnpm verify"]
---
# Build graph validate on top of the adopted engine

The layer no engine ships: boundary violations, workspace dependency cycles, orphaned
code, and duplicate-shape symbols.

Duplicate-shape detection is not speculative — run 1 produced three copies of the same
session helper across three packages. Use that as the fixture.

## Acceptance

- [ ] `pnpm graph validate` exits non-zero on an illegal cross-package import
- [ ] Detects a workspace dependency cycle
- [ ] Detects duplicate-shape symbols via normalised signature hashing
- [ ] Regression fixture: the three run-1 session helpers are detected as duplicates
- [ ] Runs in under 2s warm — it joins the Stop hook, so speed is correctness
