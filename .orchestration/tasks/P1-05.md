---
id: P1-05
status: predicted
territory: graph
phase: 1
depends_on: [P1-02]
verify: ["pnpm verify"]
---
# PreToolUse hook: consult the graph before file search

Adopted from Graphify's best idea. The hook fires before every search tool call so an
agent physically cannot grep-spray. This is the context-economy mechanism made
involuntary rather than advisory.

## Acceptance

- [ ] Hook registered in .claude/settings.json
- [ ] Fires on Grep/Glob and injects relevant graph context
- [ ] Measured: files-read-before-correct-code drops vs the run-1 baseline of ~20-48
- [ ] Does not add more than 300ms to a search

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
