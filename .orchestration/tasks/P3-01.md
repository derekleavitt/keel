---
id: P3-01
status: predicted
territory: harness
phase: 3
depends_on: [P1-05]
verify: ["pnpm verify"]
---
# Extract the harness into a portable plugin

Per architecture §1: the harness must travel to projects that are not this one.
Bundle skills, subagents, hooks and rules as a Claude Code plugin.

## Acceptance

- [ ] Plugin structure with manifest
- [ ] Installs into a non-Keel project without errors
- [ ] Keel consumes its own plugin rather than duplicating config
- [ ] Documented install path in README

## Pull condition

**PREDICTED, not scheduled.** This task is a hypothesis about what Keel will need. Do not
build it because it is next in the list.

Pull it when building a testbed feature actually demands it — when friction, a repeated
mistake, or a recorded lesson names this as the fix. Then it becomes `open`.

If the testbed never demands it, that is a finding: the prediction was wrong, and the
task should be deleted rather than built.
